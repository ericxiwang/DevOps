## check k8s kustomize yaml config
 `$ kubectl kustomize <kustomization_directory>

## To apply those Resources, run kubectl apply with --kustomize or -k flag:
 `$ kubectl apply -k <kustomization_directory>