---

# tasks file for test
- name: echo test
  shell: |
    echo {{ file_name }}
- name: echo varible
  shell: |
    echo "AAAA" > {{ file_name }}
- name: setup temp
  template:
    src: test.j2
    dest: /opt/test.txt
